# API server dependencies
# Install alongside requirements.txt for the backend container
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
redis[hiredis]>=5.0.0
httpx>=0.27.0         # for async HTTP in tests
