You must respond with ONLY a JSON object. No explanation, no preamble, no markdown, no code fences. Just the raw JSON object starting with { and ending with }.
{context_block}
JSON schema to follow exactly:

{{"sector":"","target_countries":[],"hs_codes":[],"sme_revenue_cr":null,"relevant_agents":[]}}

For "relevant_agents", choose only from: {available_agents}

Rules:
- If the query asks about export opportunities in general, include all agents.
- If the query is narrow (e.g. only about schemes or only about pricing), include only relevant agents.
- If the query is a follow-up that does not restate sector or countries, infer them from the prior conversation context above.
- If no specific countries are mentioned, leave target_countries as an empty array [].

Query: {query}

JSON response:
